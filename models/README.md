# Model Artifacts

The training script saves research model artifacts here:

```text
models/property_model.json
```

The web app checks for this file. If it does not exist, the app will not claim research-grade ML predictions.

